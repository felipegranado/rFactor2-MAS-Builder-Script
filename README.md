# rFactor2-MAS-Builder-Script
A PowerShell script to batch create `.mas` files for rFactor 2 using ModMgr.exe | Script PowerShell para criar arquivos `.mas` em lote para o rFactor 2 usando o ModMgr.exe.
